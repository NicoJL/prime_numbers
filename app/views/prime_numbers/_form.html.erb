<%= form_with(model: prime_number, local: true) do |form| %>
  <% if prime_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prime_number.errors.count, "error") %> prohibited this prime_number from being saved:</h2>

      <ul>
      <% prime_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prime_number %>
    <%= form.number_field :prime_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
